preprocess:
  raw_root: dataset
  output_root: data/processed/tracknet_dataset_512x288
  adapter: tracknet_domain
  target_width: 512
  target_height: 288
  overwrite: false
  image_extension: .png
  missing_annotation_policy: invisible
  background_sample_stride: 1
  val_fraction: 0.2
  workers: 1
